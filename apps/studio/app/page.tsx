/**
 * Spectra Studio entry route.
 *
 * The actual UI lives inside `AppShell` so this page renders nothing
 * meaningful; everything is composed from the layout shell to keep
 * the route surface area minimal.
 */
export default function Page() {
  return null;
}